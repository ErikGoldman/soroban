{
  "name": "soroban-financial-planner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit --project tsconfig.json && vite build",
    "dev": "vite",
    "deploy:firebase": "yarn build && npx firebase-tools deploy --only hosting",
    "serve:firebase": "npx firebase-tools emulators:start --only hosting",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  },
  "packageManager": "yarn@1.22.22"
}
